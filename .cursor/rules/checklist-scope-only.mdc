---
description: 仅完成用户明确提及的 checklist 部分，禁止超前完成
alwaysApply: true
---

# Checklist 范围约束

依据 `docs/端到端测试练手项目_Checklist.md` 执行任务时：

- **只做用户明确点名的阶段/条目**（如「完成 Day 1」「做 2.2 测试计划」）。
- **禁止**擅自完成用户未提及的后续日、章节或条目，例如：
  - 用户只说 Day 1 → 不做 Day 2 Postman、Day 3–4 自动化、Day 5 README/交付整理等
  - 用户只说某一小节 → 不顺带做同日其他未提及小节，除非该小节完成标准明确依赖它
- **禁止**为「顺便完善」而创建未要求的目录/文件（如 Day 1 时不要建 `postman/`、`automation/` 或完善最终 README）。
- 若完成某阶段所需**最小前置**已缺失（如 `docs/` 目录），可补齐**仅该阶段交付物必需**的内容，并在回复中说明；不要扩大范围。
- 完成后只勾选**已完成且属本次范围**的 checklist 项；不要勾选未做或范围外的项。

```text
✅ 用户：「完成 Day 1」→ 只做 §2（含 2.6 完成标准内交付物）
❌ 用户：「完成 Day 1」→ 同时做 API Collection / Playwright / 最终 README
```
